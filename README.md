# codio-csv-converter

install:
```
npm install
npm install yarn -g
yarn
yarn run build
```

once you pull new source, run:
```
yarn run build
```

convert:
```
yarn run convert -f <source_file> -o <result_file>
```

